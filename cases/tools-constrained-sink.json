{
  "id": "tools-constrained-sink",
  "kind": "tool-list",
  "label": "negative",
  "expected_rules": [],
  "source": "The same shell tool as the positive case, restricted to an enumerated set and honestly annotated.",
  "fixture": {
    "tools": [
      {
        "name": "run_task",
        "description": "Runs one of a fixed set of maintenance tasks.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "command": {
              "type": "string",
              "enum": [
                "vacuum",
                "reindex",
                "rotate-logs"
              ]
            }
          }
        },
        "annotations": {
          "destructiveHint": true
        }
      }
    ]
  }
}
